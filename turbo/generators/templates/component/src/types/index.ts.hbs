import { ElementType } from "react";

/**
 * Props interface for the {{capitalize componentName}} component.
 */
export interface {{capitalize componentName}}Props extends HTMLDivElement {
  as?: ElementType;
}